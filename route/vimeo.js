// Generated by CoffeeScript 1.6.3
var CacheEntry, OAuth, apiHost, auth, callback, oauth, processRequest, sys, util, utils, videos, _;

utils = require('../lib/utils');

_ = require('underscore')._;

OAuth = require('oauth');

util = require('util');

sys = require('sys');

CacheEntry = require('../model/cacheEntry');

apiHost = 'http://vimeo.com/api/rest/v2';

processRequest = function(req, res, url, transform) {
  var options;
  options = utils.buildOptions(req, res, url, 5 * 60, transform);
  return utils.processRequest(options);
};

oauth = new OAuth.OAuth('https://vimeo.com/oauth/request_token', 'https://vimeo.com/oauth/access_token', process.env["OAUTH_VIMEO_CONSUMER_KEY"], process.env["OAUTH_VIMEO_CONSUMER_SECRET"], '1.0', 'http://localhost:8000/api/vimeo/auth/callback', 'HMAC-SHA1');

auth = function(req, res) {
  return oauth.getOAuthRequestToken(function(error, oauthToken, oauthTokenSecret, results) {
    if (error) {
      console.error("login error %s", error);
      return utils.responseData(500, "Error getting OAuth request token : " + sys.inspect(error), void 0, {
        req: req,
        res: res
      });
    } else {
      if (!req.session) {
        req.session = {};
      }
      req.session.oauthRequestToken = oauthToken;
      req.session.oauthRequestTokenSecret = oauthTokenSecret;
      return res.redirect("http://vimeo.com/oauth/authorize?oauth_token=" + req.session.oauthRequestToken + "&permission=read");
    }
  });
};

callback = function(req, res) {
  var oauthToken, oauthTokenSecret;
  oauthToken = req.session.oauthRequestToken;
  oauthTokenSecret = req.session.oauthRequestTokenSecret;
  delete req.session.oauthRequestToken;
  delete req.session.oauthRequestTokenSecret;
  return oauth.getOAuthAccessToken(oauthToken, oauthTokenSecret, req.query.oauth_verifier, function(err, oauthAccessToken, oauthAccessTokenSecret, results) {
    if (err) {
      return utils.responseData(500, "Error getting OAuth request token : " + sys.inspect(err), void 0, {
        req: req,
        res: res
      });
    } else {
      return new CacheEntry({
        'key': 'vimeo.crendentials',
        'value': {
          accessToken: oauthAccessToken,
          accessTokenSecret: oauthAccessTokenSecret
        }
      }).save(function(err) {
        if (err) {
          return utils.responseData(500, "Error getting OAuth request token : " + sys.inspect(err), void 0, {
            req: req,
            res: res
          });
        } else {
          res.redirect("/");
          return console.info("Redirected to '/'");
        }
      });
    }
  });
};

videos = function(req, res) {
  return CacheEntry.findOne({
    key: 'vimeo.crendentials'
  }, function(err, entry) {
    if (err) {
      return utils.responseData(500, "Error getting OAuth request data : " + sys.inspect(err), void 0, {
        req: req,
        res: res
      });
    } else {
      return oauth.get(apiHost + '?method=vimeo.videos.getAll&user_id=xebia&sort=newest&page=1&per_page=50&summary_response=true&full_response=false&format=json', entry.accessToken, entry.accessTokenSecret, function(err, data, resp) {
        if (err) {
          console.error(err);
          return utils.responseData(500, "", void 0, {
            req: req,
            res: res
          });
        } else {
          console.log(sys.inspect(data));
          return utils.responseData(200, "", data, {
            req: req,
            res: res
          });
        }
      });
    }
  });
};

module.exports = {
  auth: auth,
  callback: callback,
  videos: videos
};
